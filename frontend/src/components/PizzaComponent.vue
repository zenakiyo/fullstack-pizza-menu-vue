<script setup>

</script>
<template>
  <!-- <li :class='props.soldOut ? "pizza sold-out" : "pizza"'>
            <img src={props.photoName} alt={props.name} />
            <div>
                <h3>{props.name}</h3>
                <p>{props.ingredients}</p>
                <span>{props.soldOut ? "SOLD OUT" : `Price ${props.price}` }</span>
            </div>
        </li>
    ) -->
     1
</template>
